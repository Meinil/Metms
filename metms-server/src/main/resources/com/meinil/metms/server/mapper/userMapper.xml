<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.meinil.metms.server.mapper.UserMapper">
	<update id="updateTeacher">
		UPDATE tb_connect SET teacher_id = #{teacherId} WHERE stu_id = #{stuId};
	</update>
</mapper>